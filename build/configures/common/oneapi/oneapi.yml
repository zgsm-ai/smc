services:
  oneapi:
    image: ${IMAGE_ONEAPI}
    restart: always
    environment:
      TZ: "Asia/Shanghai"
    volumes:
      - ./data:/data
    ports:
      - "${PORT_ONEAPI}:3000"
    networks:
      - shenma